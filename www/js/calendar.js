define([],function(){

  //initializes calendar module, appends its content to given node
  function init($node){
    console.log('joł, me running!');
  
  }
  

  return {
    init:init
  
  }

});
